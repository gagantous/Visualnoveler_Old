<% title @user.name + "'s Playlist" %>
<% description @user.name + "'s Playlist." " Start tracking Visual Novels and discover other hidden Visual Novel gems at Visualnoveler" %>

<div class="container-fluid">
	<div class="row vnindex-row">

		
		<%= render 'layouts/vn_title',:title => "Playing" %>
		<%= render :partial => 'list', locals: {lib: @lib}  %>
	
	</div>
</div>
