<% unless @recent_ratings.blank? %>
<div class="col-md-4 col-xs-12 col-sm-12 shared-lib">
  <div class="panel panel-default"><div class="panel-body">
    <h4>Recent Ratings</h4>
    <% @recent_ratings.each do |lib| %>
    <div class="row user-recent-ratingpadding">
      <div class="col-md-3 col-xs-3">
        <%= image_tag(lib.vn.image_poster.url(:thumb),class:"img-responsive") %>
      </div>
      <div class="col-md-9 col-xs-9">
        <%= lib.vn.name %>
        <div class="progress rating-progress">
          <% percentage = number_to_percentage(lib.rating*10,precision: 0) %>
          <div class="progress-bar" role="progressbar" aria-valuenow="6" aria-valuemin="0" aria-valuemax="10" style="width: <%= percentage %>">
            <%= lib.rating %>
          </div>
        </div>
      </div>
    </div>
    <% end %>
  </div></div>
</div>

<% end %>