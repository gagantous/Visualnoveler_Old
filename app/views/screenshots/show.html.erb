<div class="container">
	<div class="row">
		<div class="col-lg-8">
  				<%= image_tag @screenshot.image.url, :class => "img-responsive" if @screenshot.image? %>
  				<p> <%= @screenshot.name %> </p>
		</div>


	</div>

</div>