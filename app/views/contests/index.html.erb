<div class="container">
	<div class="row">
		<h1>Contests</h1>
		<% @contests.each do |contest| %>
			<div class="col-md-6">
				<%= link_to contest.name,contest_path(contest) %>
			</div>
			<div class="col-md-12">
				<%= contest.details %>
			</div>
		<% end %>
	</div>
</div>