<% if user_signed_in? %>
	<% if current_user.library_entries.exists?(vn_id: @vn.id,favourite: true) %>
 		<%= link_to favourite_vn_path(@vn, type: "unfavourite"), method: :post,:class => "btn ghost-buttonimg vn-button status-fav" ,remote: true do %>
			<i class="fa fa-times ajax-fav"></i>
		<% end %>
	<% else %>
		<%= link_to favourite_vn_path(@vn, type: "favourite"),:class => "btn ghost-buttonimg vn-button status-fav", method: :post ,remote: true do %>
			<i class="fa fa-heart ajax-fav"></i>
		<% end %>
	<% end %>
<% end %> 