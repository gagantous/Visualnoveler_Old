<!DOCTYPE html>
<html>
<head>
  <title>Kawaiivn</title>
  <link href='https://fonts.googleapis.com/css?family=Poiret+One' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <%= favicon_link_tag %>

 <div class="container">
	 <nav class="navbar navbar-transparent">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#"><span class="navLogo">Visualnoveler</span></a>
         <a class="navbar-brand" href=<%= new_vn_path %>><span class="logotitle">Create VN</span></a>

         <a class="navbar-brand" href=<%= new_character_path %>><span class="logotitle">Create Character</span></a>
         <a class="navbar-brand" href=<%= new_genre_path %>><span class="logotitle">Create Genre</span></a>
  <% if user_signed_in? %>
  <li>
  <%= link_to('Logout', logout_path, :method => :delete) %>        
  </li>
<% else %>
  <li>
  <%= link_to('Login', login_path)  %>  
  </li>
  <% end %>
<% if user_signed_in? %>
 <!--- <%= link_to('Edit registration', edit_user_registration_path) %> -->
 <%= link_to "Change your password", edit_path %>
      <ul>
          <li><%= current_user.email %> <h4>hello <%= link_to current_user.name,user_path(current_user) %></h4></li>
       </ul>
  
<% else %>

  <li>
  <%= link_to('Register', register_path)  %>
  </li>
<% end %>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            
           <!-- <li><a href="#about"><span class="navword">About</span></a></li> -->
            
          </ul>
          
        </div><!--/.nav-collapse -->
      </div>
    </nav>
      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>"><%= message %></div>
      <% end %>
</div>


<%= yield %>

</body>
</html>
<!-- 
end of container -->


